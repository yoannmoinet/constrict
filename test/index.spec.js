describe('dotconf', function () {
    'use strict';

    var chai = require('chai');
    var expect = chai.expect;

    it('should have tests', function () {
        expect(0).to.equal(1);
    });
});
